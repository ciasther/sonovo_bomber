name: ci

on:
  push:
    branches: [beta]
    tags-ignore: ['**']

concurrency:
  group: bomber-rush-beta
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  test-build:
    runs-on: [self-hosted, windows, nanovo-server]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Testy, vet i build
        shell: cmd
        run: build-windows.bat
      - name: Paczka wydania
        shell: cmd
        run: |
          if exist package rmdir /s /q package
          mkdir package || exit /b 1
          copy /y dist\BomberRush.exe package\ || exit /b 1
          xcopy /e /i /y assets package\assets || exit /b 1
      - uses: actions/upload-artifact@v4
        with:
          name: BomberRush-${{ github.sha }}
          path: package
          if-no-files-found: error

  promote:
    needs: test-build
    runs-on: [self-hosted, linux, sonovo-server]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          name: BomberRush-${{ github.sha }}
          path: package
      - name: Fast-forward main i tag wydania
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          version="v$(tr -d '[:space:]' < VERSION)"
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          if git ls-remote --exit-code --tags origin "refs/tags/$version" >/dev/null; then
            echo "tag $version juz istnieje: podnies VERSION" >&2
            exit 1
          fi
          zip_name="BomberRush-$version-windows-amd64.zip"
          rm -f "$zip_name"
          (cd package && zip -qr "../$zip_name" .)
          git fetch origin main
          git checkout -B main origin/main
          git merge --ff-only "$GITHUB_SHA"
          git push origin main
          git tag -a "$version" -m "BOMBER RUSH $version" "$GITHUB_SHA"
          git push origin "$version"
          gh release create "$version" --title "BOMBER RUSH $version" --notes-file RELEASE.txt --target "$GITHUB_SHA" "$zip_name"
